# No environment variables or API keys are required.
# Text correction runs locally in the browser using bundled rules and a lexicon.
# Existing LLM_PROVIDER, OPENAI_*, ANTHROPIC_* and UPSTASH_* values are ignored.
